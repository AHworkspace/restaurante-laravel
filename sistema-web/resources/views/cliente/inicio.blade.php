@extends('layouts.app-cliente')
@section('content')
<div class="title-wrapper pt-30"><div class="title mb-30"><h2>Menú disponible ahora</h2></div></div>
<div class="row">@forelse($menusDia as $menu)<div class="col-xl-6 mb-4"><div class="card-style-3 h-100"><div class="card-content"><div class="d-flex justify-content-between align-items-center mb-3"><h4>{{ $menu->titulo }}</h4><span class="badge bg-primary">{{ $menu->tipoComida?->nombre }}</span></div>@if($menu->descripcion)<p class="text-muted">{{ $menu->descripcion }}</p>@endif<div class="table-responsive"><table class="table mb-0"><thead><tr><th>Plato</th><th class="text-end">Precio</th></tr></thead><tbody>@foreach($menu->recetas as $receta)<tr><td>{{ $receta->nombre }}</td><td class="text-end"><strong>Bs {{ number_format($receta->pivot->precio_venta ?? $receta->precio,2) }}</strong></td></tr>@endforeach</tbody></table></div></div></div></div>@empty<div class="col-12"><div class="alert alert-info">No hay menú disponible en este momento.</div></div>@endforelse</div>
@endsection
