@php use App\Models\ConfiguracionSistema as C; $nombre=C::valor('nombre_restaurante','Las Brazas'); $primario=C::valor('color_primario','#7A5C58'); $imagen=C::valor('imagen_portal_cliente')?:C::valor('imagen_portada'); @endphp
<!DOCTYPE html><html lang="es"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Clientes - {{ $nombre }}</title>@vite('resources/sass/app.scss')<style>body{background:linear-gradient(rgba(0,0,0,.5),rgba(0,0,0,.5)),url('{{ $imagen?Storage::url($imagen):asset('images/imagen de portada.jpg') }}') center/cover fixed;font-family:'{{ C::valor('tipografia','Arial') }}',sans-serif}.portal-card{width:min(420px,100%);background:rgba(255,255,255,.96);border-radius:{{ C::valor('radio_bordes',8) }}px}.btn-primary{background:{{ $primario }};border-color:{{ $primario }}}</style></head><body><main class="container min-vh-100 d-flex align-items-center justify-content-center py-4"><div class="portal-card p-4 shadow"><h1 class="h3 mb-1">{{ $nombre }}</h1><p class="text-muted mb-4">Acceso de clientes</p><form method="POST" action="{{ route('cliente.login.store') }}">@csrf<div class="mb-3"><label>Correo</label><input type="email" class="form-control" name="email" value="{{ old('email') }}" required autofocus></div><div class="mb-3"><label>Contraseña</label><input type="password" class="form-control" name="password" required></div>@error('email')<div class="alert alert-danger">{{ $message }}</div>@enderror<button class="btn btn-primary w-100">Ingresar</button></form><a class="btn btn-outline-secondary w-100 mt-3" href="{{ route('cliente.register') }}">Crear cuenta de cliente</a><a class="d-block mt-3 text-center" href="{{ route('login') }}">Acceso del personal</a></div></main></body></html>
