@extends('layouts.app')
@section('content')
<div class="title-wrapper pt-30"><div class="row align-items-center"><div class="col-md-6"><div class="title mb-30"><h2>Insumos</h2></div></div><div class="col-md-6 text-end">@can('insumos.crear')<a class="main-btn primary-btn btn-hover" href="{{ route('insumos.create') }}"><i class="lni lni-plus"></i> Nuevo insumo</a>@endcan</div></div></div>
<div class="card-style-3 mb-30"><div class="card-content"><form class="row g-2 mb-4"><div class="col-md-5"><input class="form-control" name="buscar" value="{{ request('buscar') }}" placeholder="Buscar por nombre"></div><div class="col-md-2"><button class="btn btn-primary">Buscar</button></div></form><div class="table-responsive"><table class="table table-hover"><thead><tr><th>ID</th><th>Nombre</th><th>Descripcion</th><th>Unidad general</th><th>Presentaciones</th><th>Acciones</th></tr></thead><tbody>
@forelse($insumos as $insumo)<tr><td>{{ $insumo->id }}</td><td><strong>{{ $insumo->nombre }}</strong></td><td>{{ $insumo->descripcion?:'-' }}</td><td>{{ $insumo->unidad_medida?->nombre?:'-' }} @if($insumo->unidad_medida)<small>({{ $insumo->unidad_medida->abreviatura }})</small>@endif</td><td>{{ $insumo->presentaciones->count() }}</td><td><div class="d-flex gap-2"><a class="btn btn-outline-primary btn-sm" href="{{ route('insumos.show',$insumo) }}"><i class="lni lni-layers"></i> Presentaciones</a>@can('insumos.editar')<a class="btn btn-warning btn-sm" href="{{ route('insumos.edit',$insumo) }}" title="Editar insumo"><i class="lni lni-pencil"></i></a>@endcan @can('insumos.eliminar')<form method="POST" action="{{ route('insumos.destroy',$insumo) }}" onsubmit="return confirm('Eliminar este insumo?')">@csrf @method('DELETE')<button class="btn btn-danger btn-sm"><i class="lni lni-trash-can"></i></button></form>@endcan</div></td></tr>@empty<tr><td colspan="6" class="text-center">No hay insumos registrados.</td></tr>@endforelse
</tbody></table></div></div></div>
@endsection

