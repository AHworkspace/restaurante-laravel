@extends('layouts.app')
@section('content')
<div class="title-wrapper pt-30"><div class="row align-items-center"><div class="col-md-6"><div class="title mb-30"><h2>Tipos de produccion</h2></div></div><div class="col-md-6 text-end"><button class="main-btn primary-btn btn-hover" data-bs-toggle="modal" data-bs-target="#crear"><i class="lni lni-plus"></i> Nuevo tipo</button></div></div></div>
<div class="card-style-3 mb-30"><div class="card-content"><div class="table-responsive"><table class="table table-hover"><thead><tr><th>ID</th><th>Nombre</th><th>Descripcion</th><th>Orden</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@foreach($tipos as $tipo)<tr><td>{{ $tipo->id }}</td><td><strong>{{ $tipo->nombre }}</strong></td><td>{{ $tipo->descripcion?:'-' }}</td><td>{{ $tipo->orden }}</td><td><span class="badge {{ $tipo->activo?'bg-success':'bg-secondary' }}">{{ $tipo->activo?'Activo':'Inactivo' }}</span></td><td><button class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#editar{{ $tipo->id }}"><i class="lni lni-pencil"></i></button> <form class="d-inline" method="POST" action="{{ route('tipos-produccion.destroy',$tipo) }}" onsubmit="return confirm('Eliminar este tipo?')">@csrf @method('DELETE')<button class="btn btn-danger btn-sm"><i class="lni lni-trash-can"></i></button></form></td></tr>@endforeach</tbody></table></div></div></div>
@php($ediciones=$tipos->prepend(null)) @foreach($ediciones as $tipo)<div class="modal fade" id="{{ $tipo?'editar'.$tipo->id:'crear' }}" tabindex="-1"><div class="modal-dialog"><div class="modal-content"><form method="POST" action="{{ $tipo?route('tipos-produccion.update',$tipo):route('tipos-produccion.store') }}">@csrf @if($tipo)@method('PUT')@endif<div class="modal-header"><h5 class="modal-title">{{ $tipo?'Editar tipo':'Nuevo tipo' }}</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><label class="form-label">Nombre</label><input class="form-control mb-3" name="nombre" value="{{ $tipo?->nombre }}" required><label class="form-label">Descripcion</label><input class="form-control mb-3" name="descripcion" value="{{ $tipo?->descripcion }}"><label class="form-label">Orden</label><input type="number" min="0" class="form-control mb-3" name="orden" value="{{ $tipo?->orden??0 }}" required><label class="form-check"><input class="form-check-input" type="checkbox" name="activo" value="1" @checked($tipo?->activo??true)> Activo</label></div><div class="modal-footer"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button><button class="btn btn-primary">Guardar</button></div></form></div></div></div>@endforeach
@endsection

